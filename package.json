{
  "name": "inkdrop-mermaid-workspace",
  "private": true,
  "description": "Workspace for the Inkdrop Mermaid plugin and the @inkdropapp/mermaid rendering package",
  "license": "MIT",
  "repository": "https://github.com/inkdropapp/inkdrop-mermaid",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "format": "oxfmt",
    "lint": "oxlint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "oxfmt": "^0.60.0",
    "oxlint": "^1.75.0"
  }
}
